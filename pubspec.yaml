name: number_words_reader
description: A Package to convert numbers to Arabic or English words.
version: 0.0.3
homepage: https://github.com/moatasem-alhilali/number_words_reader.git

environment:
  sdk: ">=2.19.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
